<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd">
<mapper namespace="com.yuan.dao.MPUserMapper">

    <select id="selectAllByXML" resultType="com.yuan.entity.MPUser">
        select * from t_mp_user ${ew.customSqlSegment}
    </select>

    <select id="selectMyPage" resultType="com.yuan.entity.MPUser">
        select * from t_mp_user ${ew.customSqlSegment}
    </select>

</mapper>